import {useState} from "react";
import {createFileRoute,Link,useNavigate} from "@tanstack/react-router";
import {signUp} from "@/lib/auth";
import {GlassCard} from "@/features/core/ui/GlassCard";
import {NeonButton} from "@/features/core/ui/NeonButton";
export const Route=createFileRoute("/register")({component:Register});
function Register(){const nav=useNavigate();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [done,setDone]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");try{await signUp(email,password);setDone(true);}catch(err){setError(err instanceof Error?err.message:"Registration failed");}}return <main className="min-h-screen grid place-items-center bg-[#070b14] px-6 text-slate-100"><GlassCard className="w-full max-w-md p-8"><Link to="/" className="text-sm text-slate-500 hover:text-white">← NEXUS</Link><h1 className="mt-8 text-3xl font-bold">Create access</h1><p className="mt-2 text-sm text-slate-500">Join the NEXUS campaign network.</p>{done?<div className="mt-8 rounded-xl border border-emerald-400/20 bg-emerald-400/5 p-5"><p className="font-semibold text-emerald-300">Account created.</p><p className="mt-2 text-sm text-slate-400">Check your email if confirmation is enabled.</p><button onClick={()=>nav({to:"/login"})} className="mt-5 text-sm text-sky-300">Go to login →</button></div>:<form onSubmit={submit} className="mt-8 space-y-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-sky-400/50"/><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ characters)" className="w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-sky-400/50"/>{error&&<p className="text-xs text-red-400">{error}</p>}<NeonButton type="submit" className="w-full">Create account</NeonButton></form>}<p className="mt-6 text-center text-sm text-slate-500">Already registered? <Link to="/login" className="text-sky-300">Sign in</Link></p></GlassCard></main>}