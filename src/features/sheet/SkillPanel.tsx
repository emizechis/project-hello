import {useMemo,useState} from "react";
import {SKILLS,type Skill} from "./skills";
export function SkillPanel({values,onChange,modifiers}:{values:Skill[];onChange:(v:Skill[])=>void;modifiers:Record<string,number>}){const [q,setQ]=useState("");const shown=useMemo(()=>SKILLS.filter(x=>x.toLowerCase().includes(q.toLowerCase())),[q]);return <div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar perícia..." className="mb-4 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-sky-400/50"/><div className="grid gap-2 sm:grid-cols-2">{shown.map(name=>{const item=values.find(x=>x.name===name)||{name,bonus:0,training:false};const base=modifiers[name]??0;return <div key={name} className="flex items-center gap-3 rounded-xl border border-white/10 bg-black/10 p-3"><label className="flex flex-1 items-center gap-2 text-sm"><input type="checkbox" checked={item.training} onChange={e=>onChange([...values.filter(x=>x.name!==name),{...item,training:e.target.checked}])}/>{name}</label><span className="text-xs text-slate-500">{base>=0?"+":""}{base}</span><input type="number" value={item.bonus} onChange={e=>onChange([...values.filter(x=>x.name!==name),{...item,bonus:Number(e.target.value)}])} className="w-16 rounded-lg border border-white/10 bg-black/20 px-2 py-1 text-center"/></div>})}</div></div>}