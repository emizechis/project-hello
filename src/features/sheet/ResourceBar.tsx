export function ResourceBar({label,current,max,onChange}:{label:string;current:number;max:number;onChange:(v:number)=>void}){const pct=max?Math.max(0,Math.min(100,current/max*100)):0;return <div><div className="mb-2 flex justify-between text-xs"><span className="font-semibold text-slate-300">{label}</span><span className="text-slate-500">{current} / {max}</span></div><div className="h-2 overflow-hidden rounded-full bg-white/5"><div className="h-full rounded-full bg-sky-400 transition-all" style={{width:pct+"%"}} /></div><input aria-label={label} type="range" min="0" max={max||1} value={Math.min(current,max||1)} onChange={e=>onChange(Number(e.target.value))} className="mt-2 w-full" /></div>}