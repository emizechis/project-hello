import {useMemo,useState} from "react";
const RITUALS=[["Amaldiçoar Arma","Conhecimento"],["Consumir Manancial","Sangue"],["Embaralhar","Conhecimento"],["Paralisia de Criaturas","Energia"],["Proteção contra o Paranormal","Conhecimento"],["Perturbação","Energia"],["Velocidade Mortal","Energia"]];
export function RitualPanel(){const [q,setQ]=useState("");const [added,setAdded]=useState<string[]>([]);const list=useMemo(()=>RITUALS.filter(([n,a])=>(n+" "+a).toLowerCase().includes(q.toLowerCase())),[q]);return <div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar ritual..." className="mb-4 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><div className="space-y-2">{list.map(([name,aff])=><div key={name} className="flex items-center gap-3 rounded-xl border border-white/10 p-3"><div className="flex-1"><p className="text-sm font-semibold">{name}</p><p className="text-xs text-slate-500">{aff}</p></div><button onClick={()=>setAdded(x=>x.includes(name)?x.filter(y=>y!==name):[...x,name])} className="rounded-lg border border-white/10 px-3 py-1.5 text-xs">{added.includes(name)?"Remover":"Adicionar"}</button></div>)}</div></div>}