import {useMemo,useState} from "react";
export type InventoryItem={id:string;name:string;quantity:number;weight:number;equipped:boolean};
const ITEMS=["Kit Médico","Lanterna","Munição","Notebook","Rádio","Corda","Kit de Ferramentas","Colete Balístico"];
export function InventoryPanel({items,onChange}:{items:InventoryItem[];onChange:(v:InventoryItem[])=>void}){const [q,setQ]=useState("");const filtered=ITEMS.filter(x=>x.toLowerCase().includes(q.toLowerCase()));const add=(name:string)=>onChange([...items,{id:crypto.randomUUID(),name,quantity:1,weight:1,equipped:false}]);return <div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar ou adicionar item..." className="mb-4 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><div className="flex flex-wrap gap-2">{filtered.map(x=><button key={x} onClick={()=>add(x)} className="rounded-lg border border-white/10 px-3 py-2 text-xs hover:border-sky-400/40 hover:bg-sky-400/5">+ {x}</button>)}</div><div className="mt-5 space-y-2">{items.map(item=><div key={item.id} className="flex flex-wrap items-center gap-3 rounded-xl border border-white/10 p-3"><span className="flex-1 text-sm">{item.name}</span><input type="number" min="1" value={item.quantity} onChange={e=>onChange(items.map(x=>x.id===item.id?{...x,quantity:Number(e.target.value)}:x))} className="w-16 rounded-lg bg-black/20 px-2 py-1 text-center"/><label className="text-xs text-slate-400"><input type="checkbox" checked={item.equipped} onChange={e=>onChange(items.map(x=>x.id===item.id?{...x,equipped:e.target.checked}:x))}/> equipado</label><button onClick={()=>onChange(items.filter(x=>x.id!==item.id))} className="text-xs text-red-300">remover</button></div>)}</div></div>}