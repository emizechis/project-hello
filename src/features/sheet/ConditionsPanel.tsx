import {useState} from "react";
const CONDITIONS=["Abalado","Agarrado","Atordoado","Caído","Cego","Confuso","Desprevenido","Doente","Fraco","Impedido","Inconsciente","Surdo"];
export function ConditionsPanel(){const [active,setActive]=useState<string[]>([]);return <div className="flex flex-wrap gap-2">{CONDITIONS.map(c=>{const on=active.includes(c);return <button key={c} onClick={()=>setActive(x=>on?x.filter(y=>y!==c):[...x,c])} className={`rounded-full border px-3 py-2 text-xs transition ${on?"border-red-400/50 bg-red-400/10 text-red-300":"border-white/10 bg-white/5 text-slate-400 hover:border-white/20"}`}>{c}</button>})}</div>}