import {useState} from "react";
import {WEAPONS,type Weapon} from "./weapons";
export function WeaponPanel({items,onChange}:{items:Weapon[];onChange:(v:Weapon[])=>void}){const [q,setQ]=useState("");const shown=WEAPONS.filter(x=>x.name.toLowerCase().includes(q.toLowerCase()));return <div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar arma..." className="mb-4 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3"/><div className="flex flex-wrap gap-2">{shown.map(w=><button key={w.id} onClick={()=>onChange([...items,{...w,id:w.id+"-"+crypto.randomUUID()}])} className="rounded-lg border border-white/10 px-3 py-2 text-xs hover:border-violet-400/40">+ {w.name}</button>)}</div><div className="mt-5 grid gap-3">{items.map(w=><div key={w.id} className="rounded-xl border border-white/10 bg-black/10 p-4"><div className="flex items-start justify-between"><div><h3 className="font-semibold">{w.name}</h3><p className="text-xs text-slate-500">{w.category} · {w.extra}</p></div><button onClick={()=>onChange(items.filter(x=>x.id!==w.id))} className="text-xs text-red-300">remover</button></div><div className="mt-4 grid grid-cols-3 gap-2 text-xs"><div><span className="text-slate-500">Ataque</span><p className="font-semibold">+{w.attack}</p></div><div><span className="text-slate-500">Dano</span><p className="font-semibold">{w.damage}</p></div><div><span className="text-slate-500">Crítico</span><p className="font-semibold">{w.critical}</p></div></div></div>)}</div></div>}