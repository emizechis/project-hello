import {useState} from "react";
import {clearSession} from "@/lib/session";
import {useNavigate} from "@tanstack/react-router";
export function UserMenu(){const [open,setOpen]=useState(false);const nav=useNavigate();return <div className="relative"><button onClick={()=>setOpen(v=>!v)} className="flex items-center gap-2 rounded-xl border border-white/10 bg-white/5 px-3 py-2 text-sm hover:border-sky-400/30"><span className="grid h-7 w-7 place-items-center rounded-lg bg-violet-500/15 text-violet-300">U</span><span className="hidden sm:block">Operator</span></button>{open&&<div className="absolute right-0 top-12 z-50 w-44 rounded-xl border border-white/10 bg-[#0b1120] p-2 shadow-2xl"><button onClick={()=>{clearSession();nav({to:"/login"})}} className="w-full rounded-lg px-3 py-2 text-left text-sm text-red-300 hover:bg-red-400/10">Sign out</button></div>}</div>}